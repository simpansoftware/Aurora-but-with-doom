#!/bin/bash

clear
source /usr/share/aurora/functions
cd /root
tar xzf .grug.tar.gz
tput civis
tput cup 0 0
for frame in $(ls grug/frame_*.txt | sort); do
    tput cup 0 0
    cat "$frame" | center
done
tput cnorm
cd /
rm -rf /root/grug