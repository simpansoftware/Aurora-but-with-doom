#!/bin/busybox sh

set -e
trap '' INT SIGINT

dev_partition=$(blkid | grep 'LABEL="Aurora"' | awk -F: '{print $1}')
dev=$(echo "$dev_partition" | sed -r 's/p?[0-9]+$//')

growpart "$dev" 4
resize2fs "$dev_partition"
echo "Root filesystem expanded."
