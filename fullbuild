#!/bin/bash

apt install wget curl bash e2fsprogs gdisk cgpt rsync

board=$1

wget -q "https://dl.cros.download/files/${board}/${board}.zip" -O "./${board}.zip" || exit 1
unzip "./${board}.zip"
shim=$(unzip -Z1 "./${board}.zip")
rm ./${board}.zip
./Aurora $shim
rm $shim