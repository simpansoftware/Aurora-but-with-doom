#!/bin/sh

printf "\033]input:on\a"
printf "\033[?25h"

/bin/busybox --install -s /bin
rm -rf /init

clear

tty=/dev/pts/0
setsid sh -c "exec script -afqc 'while :; do /bin/sh || :; done' /dev/null <${tty} >>${tty} 2>&1"